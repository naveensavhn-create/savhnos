# PostgreSQL connection string (Neon in production, local Postgres in dev)
DATABASE_URL="postgresql://user:password@localhost:5432/savhnos"

# Auth.js — generate with: openssl rand -base64 32
AUTH_SECRET=""
AUTH_URL="http://localhost:3000"

# Optional — AI features (roadmap)
OPENAI_API_KEY=""
GOOGLE_API_KEY=""

# Optional — transactional email (roadmap)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASSWORD=""

# Optional — file uploads (roadmap)
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""
